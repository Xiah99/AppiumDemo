package Util;

import io.appium.java_client.AppiumDriver;

import java.util.Map;

// 封装滑动屏幕操作
public class SwipeScreen {
    // 获取屏幕尺寸
    private static Map<String, Double>  getScreenSize(AppiumDriver driver){
        driver.
    }
}
